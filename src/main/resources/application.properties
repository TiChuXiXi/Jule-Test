spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=100MB

# Optional: Configure a location for storing temporary files during multipart processing
# spring.servlet.multipart.location= # Defaults to a temp directory

# Optional: Enable multipart handling
spring.servlet.multipart.enabled=true # Default is true
